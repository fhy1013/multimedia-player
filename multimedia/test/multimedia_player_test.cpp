#include <gtest/gtest.h>

TEST(multimediaPlayerTest, testMultimedia) {
    // current path is /multimedia player/build/multimedia/test when  CMake Run Tests
    std::string media = "../../../resource/media/test.mp4";

    EXPECT_TRUE(true);
}
